if sl_hastag rough $system.self
    TSSD_ModTint tamarind 1
endif

if sl_hastag femdom $system.self
    TSSD_ModTint monze 1
endif

if sl_hastag spanking $system.self
    TSSD_ModTint geraldine 1
endif

if sl_hastag love $system.self || sl_hastag loving $system.self || sl_hastag romance $system.self
    TSSD_ModTint carnation 1
endif